# Production Line Resource Scheduling System - Python Requirements
sympy>=1.12
pandas>=2.0.0
matplotlib>=3.7.0
# Note: sqlite3, multiprocessing, socket, tkinter are built into the Python standard library.
